import React, { PropTypes } from 'react';
import { Text, 
         View, 
         StyleSheet } from 'react-native';

const ChatMessage = ({ userId, messageUserId, message, mediaSource }) => (
  <View style={styles.chat}>
    
  </View>
);

const styles = StyleSheet.create({
  chat: {

  },  
});

export default ChatMessage;
